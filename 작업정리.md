# 작업 정리

## 버그 수정

| 파일 | 수정 내용 |
|------|-----------|
| `train.py:85` | `return None, None` → `return None, None, None` (3개 언패킹 오류) |
| `train.py:59` | `groupby('hour').shift(-1)` → `groupby('date').shift(-1)` (overflow 계산 로직 오류) |
| `preprocess.py` | `build_dataset()` 마지막에 `weather.csv` merge 추가 (날씨 피처 누락) |

## 신규 생성

| 파일 | 내용 |
|------|------|
| `server.py` | FastAPI 추론 서버 — `/predict`, `/predict/all`, `/stops`, `/health` 엔드포인트, 날씨 자동 fetch, 포화 처리 로직 포함 |

## 기타 수정

| 파일 | 수정 내용 |
|------|-----------|
| `.gitignore` | `weather.csv`, `preprocessed.csv`, `models/`, `*.pkl` 추가 |
| `preprocess.py` | `HOLIDAYS`에서 `2026_05_05` 제거 (데이터 범위 밖) |

---

## 남은 작업 (파이프라인 담당)

- [ ] `requirements.txt` 작성
- [ ] 기상청 단기예보 API → `server.py` `fetch_weather()` 교체
- [ ] 실행 순서 스크립트 or README 정리
