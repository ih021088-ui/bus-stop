# Git 가이드 - Git이 뭐고 왜 쓰는가

> Git 처음이라면 이 문서 먼저 읽어. `git_rule.md`는 우리 프로젝트 규칙이고 이 문서는 기본 개념 설명이야.

---

## 1. Git이 뭐야?

Git은 **코드의 변경 이력을 기록하는 도구**야.

워드에서 "다른 이름으로 저장"을 여러 번 해본 적 있지?

```
버스혼잡_최종.py
버스혼잡_최종_수정.py
버스혼잡_진짜최종.py
버스혼잡_진짜진짜최종.py
```

Git은 이걸 자동으로 관리해줘. 파일을 하나만 유지하면서도 **모든 변경 이력**을 기억하고, 언제든 과거로 돌아갈 수 있어.

---

## 2. 왜 "브랜치(branch)"라고 해?

branch = **나뭇가지**라는 뜻이야.

나무를 생각해봐:

```
        🌿 feat/data-pipeline   (정인이 데이터 수집 작업 중)
       /
      /   🌿 feat/backend-api   (승민이 FastAPI 작업 중)
     /   /
🌳 main ── dev ────────────────  (팀 통합본)
              \
               \  🌿 feat/frontend-map  (승주가 지도 UI 작업 중)
                \
                 🌿 feat/validation     (성빈이 검증 작업 중)
```

- **줄기(main/dev)** = 모든 코드가 모이는 본체
- **가지(branch)** = 각자 독립적으로 뻗어나가는 작업 공간
- **각 가지는 서로 영향을 주지 않아** → 내 가지에서 실수해도 다른 가지는 안전

나무 가지처럼 **줄기에서 갈라져 나온다**고 해서 "branch"라고 불러.

---

## 3. 왜 브랜치 관리를 해야 해?

### 브랜치 없이 5명이 작업하면?

```
❌ 모두 같은 코드에서 직접 작업

정인이 tago_client.py 수정 → push
승민이 같은 파일 수정 → push → 충돌!
승주가 실험적 코드 추가 → 전체 서비스 다운
성빈이 되돌리려고 함 → 정인, 승민 작업까지 같이 사라짐
```

**결과**: 매일 충돌, 서로 코드 덮어쓰기, 롤백 불가능

### 브랜치로 작업하면?

```
✅ 각자 독립된 가지에서 작업

정인은 feat/data-pipeline 에서 API 수집 코드 작업
승민은 feat/backend-api 에서 FastAPI 엔드포인트 작업
승주는 feat/frontend-map 에서 지도 UI 작업
성빈은 feat/validation 에서 검증 스크립트 작업

서로의 작업에 영향 ZERO
```

**결과**: 충돌 없음, 안전한 실험, 깔끔한 코드 관리

---

## 4. 주요 Git 용어 쉽게 설명

### 기본 용어

| 용어 | 쉬운 설명 | 비유 |
|------|-----------|------|
| **Repository (저장소)** | 프로젝트 전체가 담긴 폴더 | 팀 공유 드라이브 |
| **Commit (커밋)** | "이 시점의 코드를 저장" | 게임 세이브 포인트 |
| **Branch (브랜치)** | 독립된 작업 공간 | 나뭇가지 |
| **Checkout** | 다른 브랜치로 이동 | 다른 작업 공간으로 이동 |
| **Push** | 내 작업을 GitHub에 올림 | 클라우드에 업로드 |
| **Pull** | GitHub에서 최신 코드 받기 | 클라우드에서 다운로드 |

### 협업 용어

| 용어 | 쉬운 설명 | 비유 |
|------|-----------|------|
| **Pull Request (PR)** | "내 작업을 dev에 합쳐주세요" 요청 | 결재 올리기 |
| **Merge (머지)** | 두 브랜치를 합치기 | 두 문서를 하나로 통합 |
| **Conflict (충돌)** | 같은 부분을 다르게 수정해서 Git이 혼란 | 같은 칸에 두 사람이 다른 값 적음 |
| **Review (리뷰)** | PR을 검토하는 것 | 결재 전 내용 확인 |
| **Clone** | 프로젝트를 내 컴퓨터에 복사 | 공유 폴더를 내 PC에 복사 |

---

## 5. 브랜치의 생명주기

```
① 생성: dev에서 새 가지를 만든다
   git checkout dev
   git checkout -b feat/my-feature

② 작업: 내 가지에서 코딩한다
   (코드 수정 → commit → push 반복)

③ PR: 완성되면 dev에 합쳐달라고 요청한다
   (GitHub 웹에서 Pull Request 생성)

④ 리뷰: 조장이 코드를 확인한다
   (문제 있으면 수정 요청 → 수정 후 다시 push)

⑤ 머지: 조장이 승인하면 dev에 합쳐진다

⑥ 삭제: 합쳐진 가지는 삭제한다
   (GitHub에서 "Delete branch" 클릭)
```

---

## 6. main vs dev 왜 2개야?

```
main  = 완성품이 있는 곳 (발표 때 보여주는 최종 버전)
dev   = 개발 중인 곳 (팀원들 작업이 모이는 곳)
```

|  | main | dev |
|--|------|-----|
| 용도 | 발표용 최종 버전 | 개발 통합 |
| 안정성 | 항상 안정 | 가끔 불안정할 수 있음 |
| 누가 관리 | 조장만 | 조장 + 팀원 PR |
| 직접 push | 절대 금지 | 금지 (PR로만) |

**왜 나누냐?**
- dev에서 버그가 나도 main(발표용 버전)은 안전
- dev에서 충분히 테스트한 후에 main으로 최종 정리

---

## 7. Git vs GitHub 차이

|  | Git | GitHub |
|--|-----|--------|
| 뭐야 | 코드 버전 관리 도구 | Git 프로젝트를 올리는 웹사이트 |
| 어디서 | 내 컴퓨터에서 실행 | 인터넷 (github.com) |
| 비유 | 카메라 (사진 찍기) | 클라우드 앨범 (사진 공유) |

```
내 컴퓨터 (Git)  ←→  GitHub (원격 저장소)
   commit              push / pull
   (로컬 저장)          (클라우드 동기화)
```

---

## 8. 처음 세팅하는 법 (clone부터 시작)

```bash
# 1. 레포 내 컴퓨터로 복사
git clone https://github.com/우리레포주소.git
cd bus-congestion

# 2. dev 브랜치로 이동
git checkout dev

# 3. 내 브랜치 생성 (예: 정인이라면)
git checkout -b feat/data-pipeline

# 4. 잘 만들어졌는지 확인
git branch
# * feat/data-pipeline  ← 이게 현재 위치
#   dev
#   main
```

---

## 9. 자주 하는 실수 & 해결

### "어디 브랜치에 있는지 모르겠어"
```bash
git branch          # * 표시가 현재 브랜치
```

### "다른 브랜치에서 작업해버렸어"
```bash
git stash                          # 변경사항 임시 저장
git checkout feat/내브랜치          # 올바른 브랜치로 이동
git stash pop                      # 변경사항 복원
```

### "커밋 메시지를 잘못 썼어"
```bash
git commit --amend -m "올바른 메시지"   # 직전 커밋만 수정 가능
```

### "파일 수정을 되돌리고 싶어"
```bash
git checkout -- 파일명    # 해당 파일만 마지막 커밋 상태로 복원
```

### "뭘 수정했는지 보고 싶어"
```bash
git status              # 수정된 파일 목록
git diff                # 수정된 내용 상세
```

---

## 요약

> **Git** = 코드 변경 이력을 저장하는 도구
> **Branch** = 나뭇가지처럼 갈라져 나온 독립 작업 공간
> **브랜치 관리 이유** = 5명이 충돌 없이 동시에 작업하려고
> **GitHub** = Git 프로젝트를 공유하는 클라우드
> **PR** = "내 작업 완성했으니 합쳐주세요" 요청
